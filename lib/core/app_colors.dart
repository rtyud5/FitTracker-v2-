import 'package:flutter/material.dart';

class AppColors {
  // Mint green background used in Journal top and Profile header
  static const Color mintBackground = Color(0xFFCEE8E0);
  
  // Primary dark text color
  static const Color darkText = Color(0xFF2C3E3A);
  
  // Muted text color for secondary labels
  static const Color mutedText = Color(0xFF82948E);

  // Background for white cards
  static const Color cardBackground = Colors.white;

  // Selected icon and primary action color (dark green)
  static const Color primaryAction = Color(0xFF10463A);

  // Tab indicator / active tab text color
  static const Color activeTab = Color(0xFF206D56);

  // Colors for macros
  static const Color fatColor = Color(0xFFFFD580);     // Light orange/yellow
  static const Color proteinColor = Color(0xFFCEE8E0); // Mint green like background (from screenshot)
  static const Color carbsColor = Color(0xFFE5CCFF);   // Light purple
  static const Color fiberColor = Color(0xFFFFE5B4);   // Peach/orange light
  
  static const Color fireIconColor = Color(0xFFFF6B6B);
}
